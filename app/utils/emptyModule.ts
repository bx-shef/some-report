/**
 * Пустой модуль вместо необязательных зависимостей `jspdf` (`html2canvas`, `canvg`, `dompurify`):
 * они нужны только `jsPDF.html()`, который отчёт не вызывает, а без алиаса сборщик клал бы их в
 * статику отдельными чанками — 380 КБ кода, который никогда не выполнится. `html2canvas` к тому же
 * падает на `oklch` Tailwind v4 — ровно поэтому снимок делает `html-to-image`.
 */
export default {}
